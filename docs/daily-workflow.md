# Daily Workflow System

This document covers the daily loop that powers the Brain personal assistant system: how the nightly EOD generates tomorrow's plan, how the morning review confirms it, and how automated orchestration ties everything together.

---

## The Daily Loop

The system operates on a continuous daily cycle:

```
 11:30 PM  EOD Pipeline (automated)
           ├── Phase 1: Gather data (Fathom, email, Slack, calendar)
           ├── Phase 2: Sync + hygiene (dedup, ClickUp, archiving)
           ├── Phase 3: Time tracking (Rize classification)
           ├── Phase 4: Daily note (permanent record)
           └── Phase 5: Generate Tomorrow's Today.md
                |
                v
  8:00 AM  Read Today.md (pre-built plan)
  8:00 AM  Run /morning (interactive 3-5 min review)
  8:05 AM  Deep Work 1 begins
                |
               ...  Day happens (meetings, tasks, Slack, email)
                |
                v
 11:30 PM  EOD Pipeline runs again
```

The key insight: **tomorrow's plan is ready before you wake up**. The morning review takes 3-5 minutes of confirming a pre-built plan, not 30 minutes of figuring out what to do.

---

## Today.md: The Daily Plan

`Inbox/Today.md` is an ephemeral file, overwritten every night by EOD Phase 5. It contains everything you need to start the day.

### Structure

```markdown
# Today -- Monday, March 17, 2026

## Schedule
| Time | Block | Notes |
|------|-------|-------|
| 8:00 | Morning review | Review this file, adjust if needed |
| 8:05 | Deep Work 1 | [Client A] API integration |
| 12:00 | Lunch | |
| 12:30 | Nap | Alarm 12:55 |
| 1:00 | [Contact Name] sync | [Client B], 30 min |
| 1:30 | [Contact Name] check-in | [Client A], 30 min |
| 2:30 | Deep Work 2 | Admin, async comms, reviews |
| 5:30 | Wind down | |

## Morning Exceptions
None. Full deep work block protected.

## Tasks

### Deep Work 1 (8:35 - 12:00)
- [ ] **[Client A]** Complete API integration for checkout flow <!-- src:Inbox/ClientA.md|Complete API integration for checkout -->
- [ ] **[Client B]** Review staging deployment <!-- src:Inbox/ClientB.md|Review staging deployment -->

### Meeting Window (1:00 - 2:00)
- [ ] 1:00 [Contact Name] sync ([Client B], 30 min) <!-- type:meeting -->
- [ ] 1:30 [Contact Name] check-in ([Client A], 30 min) <!-- type:meeting -->

### Deep Work 2 (2:30 - 5:30)
- [ ] **[Client C]** Reply to email re: timeline <!-- src:Inbox/ClientC.md|Reply to email re: timeline -->
- [ ] **[Cross-Client]** Update ClickUp statuses <!-- src:Inbox/Incoming.md|Update ClickUp statuses -->

### Carried Forward
_(empty on fresh day; populated if items rolled from yesterday)_

## Meeting Prep
### 1:00 -- [Contact Name] sync
**Attendees:** [Contact Name], [Your Name]
**Context:** Weekly technical sync on [Client B] project
**Recent activity:** [[2026-03-14 - Client B Weekly Sync]]
**Strategic context:** Ship v2 by end of month
**Prep needed:**
- Review PR feedback from last week
- Confirm staging deploy status

## Deadline Radar (Next 7 Days)
| Deadline | Client | Task | Status |
|----------|--------|------|--------|
| Mar 19 | [Client A] | API integration complete | In progress |
| Mar 21 | [Client B] | v2 launch | On track |

## This Week's North Star
**[Client A]:** Ship checkout flow integration
**[Client B]:** v2 launch prep
**[Client C]:** Clear backlog of async requests
**Personal:** Not yet set

## Team Priorities
> Copy/paste to team in Slack each morning:

**Primary focus:**
- [Client A]: Complete frontend for checkout flow
- [Client B]: Fix staging deploy blockers

**Secondary (if time allows):**
- [Client C]: Update documentation

**Blockers to flag:**
- Waiting on API credentials from [Contact Name]

## Adjustments
> Morning review -- note any changes here:
- [ ] _(e.g., "Moved [Client A] to after lunch, [Client C] bug more urgent")_

---
*Generated by EOD Phase 5 at 11:47 PM [TZ]*
```

### Conventions

#### Source Tags
Every work task includes an invisible HTML comment linking back to its source:
```
- [ ] **[Client A]** Task description <!-- src:Inbox/ClientA.md|fingerprint text -->
```

The fingerprint is a 30-40 character substring from the task text in the source file, unique enough to identify the exact task via substring match. This enables the morning review to trace tasks back to their origin and the carry-forward system to track which tasks persist across days.

#### Meeting Tags
Calendar meetings use a different tag:
```
- [ ] 1:00 Meeting Title (Client, duration) <!-- type:meeting -->
```
Meetings don't carry forward (they're either attended or not).

#### Carry-Forward Tags
Items that weren't completed yesterday and are re-selected for today:
```
- [ ] **[Client A]** Overdue task <!-- src:Inbox/ClientA.md|fingerprint --><!-- carried:2 -->
```
The number increments each day. Items carried for 3+ days surface as a concern during the morning review.

#### Obsidian Wiki-Links
References to vault files use `[[wiki-links]]` so you can click through directly in Obsidian:
```
**Recent activity:** [[2026-03-14 - Client B Weekly Sync]]
```

---

## /morning: Interactive Review

The `/morning` command is designed for minimal friction. On a clean day, it completes in 3 taps.

### Flow

```
Step 1: Load Today.md
        ├── Missing? Offer to generate
        ├── Stale date? Offer to regenerate
        └── Valid? Proceed
            |
            v
Step 2: Summary (8-10 lines)
        "Good morning. Tuesday, March 18.
         3 meetings today -- first at 1:00, last at 2:30
         Full deep work block protected
         Top priority: [Client A] API integration
         No imminent deadlines"
            |
            v
Step 3: Client Review (batch)
        Present ALL clients at once:
        ├── [Client A]: 2 tasks, ~2 hrs, looks clean
        ├── [Client B]: 1 task + meeting, ⚠ staging unverified
        └── [Client C]: 1 task, ~30 min
        Capacity: ~4.5 hrs needed, ~5 hrs available
            |
            v
        AskUserQuestion: "Any clients need adjustments?"
        ├── "All good" ──────────> Step 4
        ├── "[Client B]" ────────> Follow-up questions
        └── "Other" (free text) ─> Parse and apply
            |
            v
Step 4: Goal Check
        ├── All current? Skip (no output)
        └── Stale goals detected?
            AskUserQuestion: "Refresh now?"
            ├── "Skip" ──────> Step 5
            └── "Quick refresh" > Collect + update
            |
            v
Step 5: Calendar Time Blocks
        Propose blocks:
        | 8:05-8:20  | LinkedIn |
        | 8:20-8:35  | Team priorities |
        | 8:35-12:00 | Deep Work 1: [Client A], [Client B] |
        | 2:30-5:30  | Deep Work 2: [Client C], admin |
            |
        AskUserQuestion: "Create these blocks?"
        ├── "Create all" ────> Create on Google Calendar
        ├── "Adjust first" ──> Modify then create
        └── "Skip blocks" ──> No calendar events
            |
            v
Step 6: Send-off
        "Plan locked. 4 time blocks created.
         First meeting at 1:00 ([Contact Name] sync).
         [Client A] API integration starts now in Deep Work 1."
```

### Design Principles

- **Every decision uses `AskUserQuestion`** with selectable options. No free-typing required for common choices.
- **Batch then select.** Show everything at once, then ask which items need changes. Don't walk through clients one by one.
- **Flags, not nagging.** Stale goals get mentioned once with a skip option. No blocking, no guilt.
- **Overcommitment guard.** If estimated work exceeds available deep work time, force a prioritization conversation before creating blocks.
- **Time block granularity.** Group related small tasks into 30-90 minute blocks. Don't create 15-minute blocks for everything.

---

## EOD Pipeline Architecture

The end-of-day runs as a multi-phase pipeline, each phase in a fresh Claude Code context. This avoids context overflow on busy days.

### Why Phases?

A monolithic `/eod` command works fine on light days (2 calls, 20 Slack messages). On heavy days (5+ calls, 100+ Slack messages, complex email threads), the single context fills up and items start getting lost to context compression. Splitting into phases means each phase starts fresh and writes to disk for the next phase to pick up.

### Phase Architecture

```
eod-cron.sh (launchd trigger)
    |
    v
eod-runner.sh (orchestrator)
    |
    ├── Phase 1: claude -p "/eod-gather" ──> manifest + inbox files + transcripts
    ├── Phase 2: claude -p "/eod-sync"   ──> deduped inbox + ClickUp sync
    ├── Phase 3: claude -p "/eod-rize"   ──> time tracking labels
    ├── Phase 4: claude -p "/eod-note"   ──> Work/Daily/YYYY-MM-DD.md
    └── Phase 5: claude -p "/eod-today"  ──> Inbox/Today.md
```

Each phase:
- Gets a fresh context window (no bleed from previous phases)
- Reads its inputs from disk (manifest files, inbox files, temp files in `/tmp/`)
- Writes its outputs to disk (same locations)
- Has an independent timeout (25 min for gather, 5 min for daily note)
- Can fail without blocking later phases

### The Manifest File

The manifest (`/tmp/eod-manifest-TODAY.md`) is the audit trail:

```markdown
# EOD Manifest -- 2026-03-18

## Items

| # | Item | Client | Type | Source | Routed To | Status |
|---|------|--------|------|--------|-----------|--------|
| 1 | Review PR #234 | [Client A] | action-owner | Fathom: [Contact] call | Inbox/ClientA.md | ✓ |
| 2 | Follow up: [Team Member] to send mockups | [Client B] | followup | Fathom: [Contact] call | Inbox/ClientB.md | ✓ |
| 3 | Reply re: timeline | [Client C] | email-response | Email: [Contact]@... | Inbox/ClientC.md | ✓ |
| 4 | Staging deploy status? | [Client B] | research | Slack: DM | Inbox/ClientB.md | ✓ |
```

Phase 1 creates the manifest. Phase 2 reads it for deduplication. Phase 5 reads it for priority ranking (today's items rank higher than older inbox items).

### Failure Handling

- If Phase 1 fails, later phases still run with whatever data was written before the failure
- If Phase 2 fails, time tracking and daily note still generate
- Failures trigger macOS notifications and optional Slack DMs
- The `--phase N` flag lets you re-run a single failed phase
- The `--dry-run` flag previews what would execute

---

## Cron Automation

### launchd (macOS)

The EOD pipeline runs via a `launchd` user agent:

```xml
<key>StartCalendarInterval</key>
<array>
    <!-- Monday through Friday at 11:30 PM -->
    <dict>
        <key>Weekday</key><integer>1</integer>
        <key>Hour</key><integer>23</integer>
        <key>Minute</key><integer>30</integer>
    </dict>
    <!-- ... repeat for weekdays 2-5 -->
</array>
```

Install with:
```bash
cp com.brain.eod-runner.plist ~/Library/LaunchAgents/
launchctl load ~/Library/LaunchAgents/com.brain.eod-runner.plist
```

### Version Pinning

Claude Code auto-updates. On macOS, each new version is a new binary that needs TCC (Transparency, Consent, Control) file access permissions. If the EOD runs with an un-permissioned binary, it silently fails to read vault files.

Solution: pin a specific version:

```bash
# Pin the current version
echo "2.1.14" > ~/scripts/eod-claude-version

# The cron wrapper reads this and uses the pinned binary
PINNED_BIN="$HOME/.local/share/claude/versions/$PINNED_VER"
export EOD_CLAUDE="$PINNED_BIN"
```

When Claude updates, the cron wrapper sends a notification: "Claude 2.1.15 available. EOD pinned to 2.1.14." You then:
1. Grant the new version file access permissions
2. Update the pin: `echo "2.1.15" > ~/scripts/eod-claude-version`

### Lockfile Pattern

Prevents overlapping runs (important if a phase runs long):

```bash
LOCKFILE="/tmp/eod-runner.lock"
if [ -f "$LOCKFILE" ]; then
  LOCK_PID=$(cat "$LOCKFILE")
  if kill -0 "$LOCK_PID" 2>/dev/null; then
    echo "SKIPPED: Previous run still active"
    exit 0
  fi
  rm -f "$LOCKFILE"  # Stale lock
fi
echo $$ > "$LOCKFILE"
trap 'rm -f "$LOCKFILE"' EXIT
```

### Timeout Pattern (macOS)

macOS doesn't have GNU `timeout`. Use Perl's alarm signal:

```bash
# Timeout after 25 minutes (1500 seconds)
perl -e 'alarm shift; exec @ARGV' 1500 \
    claude -p "/eod-gather" --dangerously-skip-permissions
```

Exit code 142 (128 + SIGALRM 14) indicates a timeout.

---

## The Monolithic Alternative

If the phased pipeline is too complex to start with, a single `/eod` command works fine:

```markdown
# End of Day

## Setup
Source .env, verify date, create manifest.

## Section 1: Call Transcripts
Pull from Fathom, save, extract action items, route.

## Section 2: Tomorrow's Calendar
Fetch events, format schedule.

## Section 3: Email
Surface items needing response, route.

## Section 4: Slack
Check all workspaces, route.

## Section 5: Time Tracking
Pull Rize data, classify, generate relabel checklist.

## Section 6: Verify + Sync
Dedup, ClickUp sync, hygiene.

## Section 7: Daily Note
Generate summary.

## Section 8: Today.md
Generate tomorrow's plan.
```

Start with this. Split into phases when you notice context overflow (items getting lost, sections being skipped, or the AI apologizing for running out of space).

---

## Key Patterns

### Route-As-You-Go

Every extracted item gets routed to its destination file immediately, not batched for later. This prevents items from being lost if:
- The AI's context gets compressed (long sessions)
- A phase fails partway through
- The session is interrupted

### Atomic Writes for iCloud

If your vault syncs via cloud storage, use Python read-modify-write instead of the AI's built-in file editor:

```python
python3 << 'PYEOF'
with open("Inbox/ClientA.md", "r") as f:
    content = f.read()
# Insert new task under Open Tasks
content = content.replace("### Open Tasks\n", "### Open Tasks\n- [ ] New task -- *from source*\n")
with open("Inbox/ClientA.md", "w") as f:
    f.write(content)
PYEOF
```

The built-in file editor reads the file, then writes it as a separate operation. If iCloud syncs a change in between, the write overwrites the sync. The Python script does both in a single process.

### Deduplication

Before adding any item, check if it already exists:
- Same task text (fuzzy match) in the same client file
- Same Slack message timestamp in the processed state file
- Same email subject + sender from the same day

When a duplicate is found, update the source note ("*also discussed 3/18*") instead of creating a new entry.

### Carry-Forward Decay

Tasks in Today.md have a carry count. This creates natural pressure:
- Day 1: Task appears fresh
- Day 2: `<!-- carried:1 -->` (noted in morning review)
- Day 3: `<!-- carried:2 -->` (flagged as concern)
- Day 4+: If it keeps getting re-selected but never done, the morning review surfaces it explicitly

Tasks that aren't re-selected simply stay in the client inbox file. They don't carry forward; they drop off Today.md and wait for a future prioritization cycle.
